/** The page's own name, beside Reviews in the Study nav. */
export const UK_STUDY_PAGE = {
  tab: "UmaKuma",
  path: "uk-study",
} as const;

/** Where the session's answer row comes from, asserted by the test. */
export const UK_STUDY_ANSWER_ROW = "StudyReviewFlashActionRow";
